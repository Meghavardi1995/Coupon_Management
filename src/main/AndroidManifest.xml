<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.india.coupon_management">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Coupon_Management"
            android:label="Coupon Management">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Distribute_Coupon"
            android:parentActivityName=".Coupon_Management"
            android:label="Distribute Coupon"/>
        <activity android:name=".Consume_Coupon" android:parentActivityName=".Coupon_Management" android:label="Consume Coupon"></activity>
    </application>

</manifest>